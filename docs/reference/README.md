# Reference Documents

These files are retained for historical and strategic context only. They are not normal implementation inputs.

Use `AGENTS.md`, `docs/contracts/`, `docs/adr/`, `docs/api/`, `docs/schemas/`, task cards, fixtures, and tests for code generation.

PDFs in this folder cannot carry inline superseded headers, so their status is defined here and in `docs/DOCS_INDEX.md`: reference-only. If a PDF conflicts with newer canonical files, the canonical files win.
