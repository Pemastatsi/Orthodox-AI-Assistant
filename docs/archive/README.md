# Archived Planning Drafts

These files are superseded. They are kept only for traceability.

Normal coding sessions must not read these files. Use `AGENTS.md`, `docs/contracts/`, `docs/adr/`, `docs/api/`, `docs/schemas/`, task cards, fixtures, and tests instead.
