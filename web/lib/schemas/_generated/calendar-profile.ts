import { z } from "zod"

export default z.object({ "version": z.string().regex(new RegExp("^[0-9]{4}-[0-9]{2}-[0-9]{2}\\.[0-9]+$")).describe("YYYY-MM-DD.NN per docs/contracts/api-versioning.md. Bumped on every change to fixedFeastCalendar or paschalion. Drives cache-key calendarVersion."), "fixedFeastCalendar": z.enum(["julian","revised_julian","gregorian"]).describe("Which calendar governs fixed feasts (e.g., Nativity, Theophany)."), "paschalion": z.enum(["julian","gregorian"]).describe("Which Paschalion governs movable feasts. Independent of fixedFeastCalendar (decision register row I)."), "displayLabel": z.union([z.string().describe("Optional admin-facing label (e.g., 'Old Calendar (Julian)')."), z.null().describe("Optional admin-facing label (e.g., 'Old Calendar (Julian)').")]).describe("Optional admin-facing label (e.g., 'Old Calendar (Julian)').").optional(), "notes": z.union([z.string().describe("Optional free-text notes for content managers; never user-facing."), z.null().describe("Optional free-text notes for content managers; never user-facing.")]).describe("Optional free-text notes for content managers; never user-facing.").optional() }).strict().describe("Inline calendar profile stored on tenants.config.calendarProfile. Carries the version string used by cache-key.md (calendarVersion field). Per decision register row I, fixed-feast and Paschalion choices are independent.")
