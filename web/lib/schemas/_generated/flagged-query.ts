import { z } from "zod"

export default z.object({ "flaggedQueryId": z.string(), "tenantId": z.string(), "userId": z.string(), "runId": z.union([z.string(), z.null()]).optional(), "queryTextRedacted": z.string().describe("Redacted query text per ADR 0005. Raw text lives only in raw_sensitive_log when the row is sensitive."), "rawSensitiveLogId": z.union([z.string().describe("FK to encrypted raw_sensitive_log row when sensitive; admin-only, audited, 30-day retention."), z.null().describe("FK to encrypted raw_sensitive_log row when sensitive; admin-only, audited, 30-day retention.")]).describe("FK to encrypted raw_sensitive_log row when sensitive; admin-only, audited, 30-day retention.").optional(), "flagReason": z.enum(["red_tier","insufficient_evidence","block_with_redirect","verifier_failed","hard_safety_trigger","user_reported"]), "sensitivityPrimary": z.union([z.string(), z.null()]).optional(), "riskFlags": z.array(z.string()).optional(), "embedding": z.union([z.array(z.number()).describe("Optional flagged-query embedding for clustering. Vector dimension matches the active embedding model."), z.null().describe("Optional flagged-query embedding for clustering. Vector dimension matches the active embedding model.")]).describe("Optional flagged-query embedding for clustering. Vector dimension matches the active embedding model.").optional(), "embeddingModel": z.union([z.string(), z.null()]).optional(), "clusterId": z.union([z.string(), z.null()]).optional(), "createdAt": z.string().datetime({ offset: true }) }).strict().describe("Captured query that hit RED, block_with_redirect, insufficient_evidence, or A6 verifier failure. Used for content gap analysis and safety review.")
