import { z } from "zod"

export default z.object({ "chunkId": z.string(), "sourceId": z.string(), "tenantId": z.string(), "text": z.string().min(1), "chunkHash": z.string().regex(new RegExp("^sha256:[0-9a-f]{64}$")), "approved": z.boolean().default(false), "visibility": z.enum(["member","scholar","admin_only","suppressed"]), "father": z.union([z.string(), z.null()]).optional(), "work": z.union([z.string(), z.null()]).optional(), "page": z.union([z.string().describe("Legacy single-page citation string. New chunks emitted under the hierarchical chunking contract populate pageStart/pageEnd instead; page may still be derived (e.g., 'pp. 12-14') for display."), z.null().describe("Legacy single-page citation string. New chunks emitted under the hierarchical chunking contract populate pageStart/pageEnd instead; page may still be derived (e.g., 'pp. 12-14') for display.")]).describe("Legacy single-page citation string. New chunks emitted under the hierarchical chunking contract populate pageStart/pageEnd instead; page may still be derived (e.g., 'pp. 12-14') for display.").optional(), "timestamp": z.union([z.string(), z.null()]).optional(), "language": z.enum(["en","el","mixed"]).default("en"), "categories": z.array(z.string()).optional(), "sectionPath": z.array(z.string()).describe("Ordered list of enclosing heading strings from document root. Empty for content preceding the first heading. Required for chunks produced under the hierarchical chunking contract (chunking-contract.md, ADR-0009).").default([]), "pageStart": z.union([z.number().int().gte(1).describe("1-indexed PDF page number of the first ParsedBlock in this chunk. Required for chunks produced under the hierarchical chunking contract."), z.null().describe("1-indexed PDF page number of the first ParsedBlock in this chunk. Required for chunks produced under the hierarchical chunking contract.")]).describe("1-indexed PDF page number of the first ParsedBlock in this chunk. Required for chunks produced under the hierarchical chunking contract.").optional(), "pageEnd": z.union([z.number().int().gte(1).describe("1-indexed PDF page number of the last ParsedBlock in this chunk. Must be >= pageStart when both are set."), z.null().describe("1-indexed PDF page number of the last ParsedBlock in this chunk. Must be >= pageStart when both are set.")]).describe("1-indexed PDF page number of the last ParsedBlock in this chunk. Must be >= pageStart when both are set.").optional(), "parentChunkId": z.union([z.string().describe("References chunks.chunkId of the parent when this chunk was produced by a sentence-boundary split inside a single oversized parent (chunking-contract.md §Chunk boundaries). NULL for top-level chunks. The join key for the Phase 2 graph traversal layer (ADR-0006)."), z.null().describe("References chunks.chunkId of the parent when this chunk was produced by a sentence-boundary split inside a single oversized parent (chunking-contract.md §Chunk boundaries). NULL for top-level chunks. The join key for the Phase 2 graph traversal layer (ADR-0006).")]).describe("References chunks.chunkId of the parent when this chunk was produced by a sentence-boundary split inside a single oversized parent (chunking-contract.md §Chunk boundaries). NULL for top-level chunks. The join key for the Phase 2 graph traversal layer (ADR-0006).").optional(), "contextPrefix": z.union([z.string().max(200).describe("Optional 50–100-token document-context summary produced at ingestion time by a ModelRoute with purpose='context_prefix' (ADR-0009 Step 4, D-MDL-002). When set, the embedding input is contextPrefix + '\\n\\n' + text and the BM25 index uses the same concatenation. The pristine text field is unaffected and remains the canonical source for A6 quote-overlap. NULL on chunks ingested before Contextual Retrieval was enabled for the tenant (corpusVersion-pinned)."), z.null().describe("Optional 50–100-token document-context summary produced at ingestion time by a ModelRoute with purpose='context_prefix' (ADR-0009 Step 4, D-MDL-002). When set, the embedding input is contextPrefix + '\\n\\n' + text and the BM25 index uses the same concatenation. The pristine text field is unaffected and remains the canonical source for A6 quote-overlap. NULL on chunks ingested before Contextual Retrieval was enabled for the tenant (corpusVersion-pinned).")]).describe("Optional 50–100-token document-context summary produced at ingestion time by a ModelRoute with purpose='context_prefix' (ADR-0009 Step 4, D-MDL-002). When set, the embedding input is contextPrefix + '\\n\\n' + text and the BM25 index uses the same concatenation. The pristine text field is unaffected and remains the canonical source for A6 quote-overlap. NULL on chunks ingested before Contextual Retrieval was enabled for the tenant (corpusVersion-pinned).").optional(), "embeddingModel": z.string().describe("Versioned embedding model used at index time, e.g., 'openai:text-embedding-3-small@2024-03'."), "embeddingDimension": z.number().int().gte(1), "corpusVersion": z.string(), "reviewNote": z.union([z.string(), z.null()]).optional(), "createdAt": z.string().datetime({ offset: true }) }).strict().describe("Indexed retrievable chunk. The Qdrant payload mirrors the same fields plus the embedding vector.")
