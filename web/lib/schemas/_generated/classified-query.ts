import { z } from "zod"

export default z.object({ "rawQuery": z.string().min(1), "reframedQuery": z.union([z.string(), z.null()]).optional(), "answerMode": z.any(), "preliminaryConfidenceTier": z.enum(["GREEN","YELLOW","RED"]).describe("A1's preliminary read of expected answerability before retrieval. The authoritative confidenceTier on EvidencePacket and VerifiedResponse is computed by A4 after evidence admission. Used only to short-circuit RED early-exit paths."), "sensitivityPrimary": z.enum(["normal","pastoral_advice","political","medical","comparative_religion","canonical_dispute","other_sensitive"]), "riskFlags": z.array(z.enum(["self_harm","medical_emergency","canonical_dispute_active","minor_protection"])).refine((arr) => arr.every((item, i) => arr.indexOf(item) == i), "All items must be unique!"), "handling": z.enum(["answer","answer_with_disclaimer","reframe_to_teaching","block_with_redirect","insufficient_evidence"]), "reframingDisclosureRequired": z.boolean().default(false), "classifierVersion": z.string().optional(), "caseClass": z.union([z.literal("fabrication_attempt"), z.literal("no_source_quote"), z.literal("political_voting"), z.literal("self_harm"), z.literal("medical_emergency"), z.literal(null)]).describe("Optional A1 pre-classification of a block_with_redirect query into its bounded-fallback case class. Disambiguates wire-identical hard-safety cases (e.g. fabrication_attempt vs no_source_quote) so the /query hard-safety path emits the correct canonical bounded text; when absent, the path falls back to risk-flag / sensitivity heuristics.").optional() }).strict()
