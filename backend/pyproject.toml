[project]
name = "orthodox-ai-backend"
version = "0.0.1"
description = "Orthodox AI Assistant — closed-corpus theological Q&A backend (FastAPI)."
requires-python = ">=3.12,<3.13"
readme = "README.md"
license = { text = "MIT" }

dependencies = [
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.30,<1",
    "pydantic>=2.7,<3",
    "pydantic-settings>=2.3,<3",
    "email-validator>=2.2,<3",
    "sqlalchemy>=2.0,<3",
    "alembic>=1.13,<2",
    "asyncpg>=0.29,<1",
    "psycopg[binary]>=3.2,<4",
    "qdrant-client>=1.10,<2",
    "redis>=5.0,<6",
    "arq>=0.26,<1",
    "anthropic>=0.39,<1",
    "openai>=1.40,<2",
    "stripe>=10.0,<13",
    "structlog>=24.1,<26",
    "cryptography>=43,<46",
    "pyjwt[crypto]>=2.8,<3",
    "httpx>=0.27,<1",
    "tenacity>=8.3,<10",
    "ulid-py>=1.1,<2",
    "pyyaml>=6.0,<7",
    "python-multipart>=0.0.9,<1",
    "pdfplumber>=0.11,<1",
    "pytesseract>=0.3.10,<1",
    "Pillow>=10.4,<12",
    "pdf2image>=1.17,<2",
    "tiktoken>=0.7,<1",
    "nltk>=3.9,<4",
    "fastembed>=0.3,<1",
]

[dependency-groups]
dev = [
    "pytest>=8.2,<9",
    "pytest-asyncio>=0.23,<1",
    "pytest-cov>=5.0,<7",
    "ruff>=0.5,<1",
    "mypy>=1.10,<2",
    "types-pyyaml>=6.0,<7",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "live_llm: test that calls a real LLM provider; requires OPENROUTER_API_KEY",
]
